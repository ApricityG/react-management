import React, { FC } from 'react';

interface Props {
  // Put your props here
}

const Notification: FC<Props> = (props) =>{ 

return (
    <div>
      // Put your JSX contents here
    </div>
  );
}

export default Notification;