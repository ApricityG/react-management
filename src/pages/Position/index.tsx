import React, { FC } from 'react';

interface Props {
  // Put your props here
}

const Position: FC<Props> = (props) =>{ 

return (
    <div>
      // Put your JSX contents here -Position
    </div>
  );
}

export default Position;