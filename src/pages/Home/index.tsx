import React, { FC,useEffect } from 'react';
interface Props {
  // Put your props here
}

const Home: FC<Props> = (props) => {
  useEffect(()=>{
  
  },[])
  return (
    <div>
      主页
    </div>
  )};

export default Home;