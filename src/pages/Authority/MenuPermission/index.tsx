import React, { FC } from 'react';

interface Props {
  // Put your props here
}

const MenuPermissions: FC<Props> = (props) =>{ 

return (
    <div>
      // Put your JSX contents here -MenuPermissions
    </div>
  );
}

export default MenuPermissions;